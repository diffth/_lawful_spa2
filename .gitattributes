# 저장소에는 항상 LF로 보관하고, 체크아웃할 때도 LF로 내려받는다.
# (core.autocrlf 설정이 사람마다 달라도 줄바꿈이 섞이지 않게 한다)
* text=auto eol=lf

# 바이너리 애셋은 변환 대상에서 제외한다.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.pdf binary
